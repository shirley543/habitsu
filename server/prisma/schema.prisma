generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum GoalType {
  NUMERIC
  BOOLEAN
}

model User {
  id    Int     @id @default(autoincrement())
  name  String? @db.VarChar(255)
  email String  @unique @db.VarChar(255)
  goals Goal[]
}

model Goal {
  id            Int         @id @default(autoincrement())
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @updatedAt
  title         String      @db.VarChar(255)
  description   String?
  colour        String      @db.VarChar(6)
  public        Boolean     @default(false)
  goalType      GoalType
  numericTarget Int?
  numericUnit   String?     @db.VarChar(20)
  entries       GoalEntry[]
  userId        Int
  user          User        @relation(fields: [userId], references: [id])
}

model GoalEntry {
  id           Int      @id @default(autoincrement())
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  entryDate    DateTime
  goalId       Int
  goal         Goal     @relation(fields: [goalId], references: [id])
  numericValue Int?
  booleanValue Boolean?
  note         String?
  // TODOs: Figure out onDelete action and onUpdate action (NoAction?)

  @@unique([goalId, entryDate])
}

// TODOs: Figure out if a Profile model is necessary (what summary stats to show)
// Activity log? Number of days filled out a goal? Number of filled out gridcells total?
